apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vijay-app.fullname" . }}
  labels:
    app: {{ include "vijay-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "vijay-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "vijay-app.name" . }}
    spec:
      containers:
        - name: vijay-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 3000
              name: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}

